import '@web3modal/polyfills';
import type { Chain, Config } from '@wagmi/core';
import type { FallbackTransport, PublicClient } from 'viem';
export interface ConfigOptions {
    projectId: string;
    chains: Chain[];
    metadata?: {
        name?: string;
        description?: string;
        url?: string;
        icons?: string[];
        verifyUrl?: string;
    };
    enableInjected?: boolean;
    enableEIP6963?: boolean;
    enableCoinbase?: boolean;
    enableEmail?: boolean;
    enableWalletConnect?: boolean;
}
export type WCDefaultWagmiConfig = Config<PublicClient<FallbackTransport, Chain>>;
export declare function defaultWagmiConfig({ projectId, chains, metadata, enableInjected, enableCoinbase, enableEIP6963, enableEmail, enableWalletConnect }: ConfigOptions): WCDefaultWagmiConfig;
